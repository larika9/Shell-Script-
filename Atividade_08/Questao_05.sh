#!/bin/bash
echo "5 - Escreva um script que exiba um arquivo parte por parte (digamos 10 linhas em cada parte), esperando por um enter, até o fim do arquivo."

echo -p "Qual arquivo que deseja ler? " arquivo

cp arquivo alterar.txt
conta_linha = arquivo | wc -l


while true
for i in $
    cat arquivo | head ${linha} 

then < arquivo

